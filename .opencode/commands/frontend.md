---
description: Implement a frontend change following the Next.js conventions
agent: frontend-expert
---

Implement the following frontend change following the conventions in `AGENTS.md` (generated types from OpenAPI, caching by tags vs user data, design tokens in `app/globals.css`, UI kit components):

$ARGUMENTS

Verify with `pnpm typecheck` and `pnpm lint` (plus `pnpm test` if logic changed).
